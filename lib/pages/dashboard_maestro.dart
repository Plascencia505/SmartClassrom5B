import 'package:flutter/material.dart';

class DashboardMaestro extends StatelessWidget {
  const DashboardMaestro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Bienvenido a la página de escuelas"));
  }
}
